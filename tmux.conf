# Use fish as our interactive + login shell
set -g default-command "fish -i -l"

# Tmux recommends using tmux-256color
set -g default-terminal "tmux-256color"

# act like vim
setw -g mode-keys vi
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R
bind-key -r C-h select-window -t :-
bind-key -r C-l select-window -t :+

# start window numbers at 1 to match keyboard order with tmux window order
set -g base-index 1
set-window-option -g pane-base-index 1

# renumber windows sequentially after closing any of them
set -g renumber-windows on

# Minimal window status format with a hint of color
set -g status-bg 'black'
setw -g window-status-format "#[fg=blue,bg=black] #I #[fg=white,bg=black]#W "
setw -g window-status-current-format "#[fg=green,bg=colour8] #I #[fg=colour15,bg=colour8]#W "

# remove administrative debris (session name, hostname, time) in status bar
set -g status-left ''
set -g status-right ''

# increase scrollback lines
set -g history-limit 10000

# Set the escape timeout to a low value, otherwise this will cause a delay when
# switching modes in neovim.
set -g escape-time 10

# Local config
if-shell "[ -f ~/.tmux.conf.local ]" 'source ~/.tmux.conf.local'
